import 'package:flutter/material.dart';

class ProjectPadding {
  static const cardPadding = EdgeInsets.all(7);
  static const categoyPadding = EdgeInsets.all(10);
  static const buttonPadding = EdgeInsets.all(20);
}
